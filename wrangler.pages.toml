# Cloudflare Pages Configuration
# This file is specifically for Cloudflare Pages deployment

name = "tinh-luong-2026"
compatibility_date = "2024-12-26"

# Build configuration for Cloudflare Pages
pages_build_output_dir = "dist"

# Note: For Cloudflare Pages deployment via dashboard:
# - Build command: pnpm run build
# - Build output directory: dist
# - Root directory: ./
# - Framework preset: Vite (or None)
#
# The _redirects and _headers files in public/ folder will be automatically
# copied to dist/ during build and used by Cloudflare Pages

