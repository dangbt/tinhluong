# Cloudflare Pages Configuration
# This file is used for Cloudflare Pages deployment

name = "tinh-luong-2026"
compatibility_date = "2024-12-26"

# Build configuration for Cloudflare Pages
# Cloudflare Pages will automatically detect the build command from package.json
# Build output directory
pages_build_output_dir = "dist"

# Environment variables (set these in Cloudflare Pages dashboard)
# [env.production.vars]
# NODE_ENV = "production"

# Note: For Cloudflare Pages:
# - Use _redirects file in public/ folder for SPA routing
# - Use _headers file in public/ folder for custom headers
# - Build command: pnpm run build (or npm run build)
# - Build output directory: dist
# - Root directory: ./
